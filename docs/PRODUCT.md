# ADL NEXUS — The Personal Sovereign Work OS

**Positioning (locked):** not a chatbot, not an agent marketplace, not a kernel OS.

> Give it an objective. It builds a plan, assembles authorized intelligence, evaluates alternatives, executes only inside constitutional bounds, records what happened, and updates auditable memory.

## Nine permanent subsystems

| Subsystem | Absorb from | Runtime status |
|-----------|-------------|----------------|
| NEXUS KERNEL | ADL-Nexus | in-tree spine |
| MEMORY | layer1 + clean-room adapter | local KV; live VSA **UNSUPPORTED** |
| REALITY | RealityOS concepts | local Reality State; live org twin **UNSUPPORTED** |
| KNOWLEDGE | census / repo graph | analysis pathway only |
| WORKFORCE | Digital Double roles | supervised board; autonomy **UNSUPPORTED** |
| RESEARCH | CFT / OLC / Ware *as lab, not propulsion* | registry + evidence |
| GOVERNANCE | ADL-Governance / claim discipline | fail-closed evaluate |
| AEGIS / CLEAN ROOM | forge-aegis, sovereign-clean-room | adapters stub-or-live-detect |
| PROVENANCE | ledger + operation log | local append-only |

Physics, trading experiments, Agent-Snake, AtomicNexusAI stay **research assets**, not runtime dependencies.

## Invariant

AI advises. It cannot execute without an explicit authorize step.
Reality is constructed **before** action.

## First integration (this tree)

`think` → Reality State + proposal  
`authorize` → constitutional boundary  
`commit` → optional local execute + provenance + memory  

Claim level remains **2**. Lifecycle remains **RESEARCH**.
